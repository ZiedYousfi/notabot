#![allow(unused)]
#![allow(clippy::all)]

//! TCP event source placeholder.
//!
//! The concrete implementation currently lives in `sources/mod.rs` as `TcpSource`.
//! This file is intentionally kept as a harmless stub to avoid breaking references during
//! refactors and to keep the module tree flexible.

/// This module is a no-op placeholder; see `sources::TcpSource` in `sources/mod.rs`.
pub(crate) mod placeholder {
    // Intentionally empty.
}
