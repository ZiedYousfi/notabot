#![allow(unused)]
#![allow(clippy::all)]

//! Directory event source placeholder.
//!
//! The concrete implementation currently lives in `sources/mod.rs` as `DirectorySource`.
//! This file is intentionally kept as a harmless stub to avoid breaking references during
//! refactors and to keep the module tree flexible.

/// This module is a no-op placeholder; see `sources::DirectorySource` in `sources/mod.rs`.
pub(crate) mod placeholder {
    // Intentionally empty.
}
